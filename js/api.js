// custom micro framework for roots_and_wings13k

var _ = {

    // distance formula
    dist : function (x1, y1, x2, y2) {

        return Math.sqrt(Math.abs(Math.pow(x1 - x2, 2)) + Math.abs(Math.pow(y1 - y2, 2)));

    }

};
